# q1. Create a tuple of 5 integers and print the first and last element.

# t = (1, 2, 3, 4, 5)

# print(t[0], t[-1])

# ----------------------------------------------------------------

# q2. What is the difference between a tuple with one element and a normal value? Show with code.

# t = (1, )  #comma makes it a tuple
# v = 1

# ------------------------------------------------------------------------

# q3. Check whether a given value exists in a tuple.

# value = int(input("Enter a number : "))
# t = (1, 2, 3, 4)

# if value in t:
#     print(f"{value} already exists in tuple")
# else:
#     print(f"{value} is a unique value!")