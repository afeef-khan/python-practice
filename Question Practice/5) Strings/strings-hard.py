# q1. Find the most frequent character in a string (ignore spaces).

# q2. Remove duplicate characters from a string while maintaining order.

# # q3. Check if two strings are anagrams (without using sorted() ). 